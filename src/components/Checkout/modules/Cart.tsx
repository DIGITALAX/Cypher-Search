import { FunctionComponent } from "react";
import { CartProps } from "../types/checkout.types";

const Cart: FunctionComponent<CartProps> = (): JSX.Element => {
  return (
    <div className="relative w-full h-[80vh] relative flex items-start justify-center"></div>
  );
};

export default Cart;
