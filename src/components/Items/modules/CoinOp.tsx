import { FunctionComponent } from "react";
import { CoinOpProps } from "../types/item.types";

const CoinOp: FunctionComponent<CoinOpProps> = ({ itemData }): JSX.Element => {
  return <div></div>;
};

export default CoinOp;
